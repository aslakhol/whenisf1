---
export interface Props {
  label: string;
  href: string;
}

const { label, href } = Astro.props;
---

<a
  class="rounded uppercase p-2 bg-brand-accent w-32 text-center drop-shadow-lg hover:bg-brand-secondary bold"
  href={href}
>
  {label}
</a>
