---
export interface Props {
  goTime: Date;
  utc?: boolean;
}

const { goTime, utc } = Astro.props;
---

<>
  {
    goTime.toLocaleDateString("en-GB", {
      timeZone: utc ? "UTC" : undefined,
      day: "numeric",
      month: "short",
      year: "numeric",
    })
  }
</>
