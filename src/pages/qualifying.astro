---
import WhenPage from "../layouts/WhenPage.astro";
import monza from "../monza.json";

const { date, time } = monza.MRData.RaceTable.Races[0].Qualifying;

const goTime = new Date(date + "T" + time);
---

<WhenPage session={"Qualifying"} goTime={goTime} />
